from __future__ import annotations

"""Version and release metadata for the LMS desktop application.

``VERSION`` is a dotted numeric version used for distribution metadata and the
Windows file version resource. ``RELEASE_LABEL`` is the human-facing milestone
label shown in the application sidebar.
"""

APP_NAME = "Landlord Management System"
ORGANIZATION_NAME = "LMS"

VERSION = "1.0.1"
VERSION_INFO = (1, 0, 1, 0)

RELEASE_LABEL = "1.0.1"
